import { ButtonConfig } from "@/types/button.type";
const soundButtonConfig: ButtonConfig = {
    x: 30,
    y: 30,
    texture: "images",
    default_texture: "sound-on.png",
    disabled_texture: "sound-off.png"
}

export { soundButtonConfig };